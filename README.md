# Agent Landing (GitHub Pages)

Static landing page for:
- WhatsApp agent (EN/FR)
- lead capture into Notion
- Stripe payment link

## Local preview

Open `index.html` in a browser.

## Publish

This repo is intended for GitHub Pages.
- If using `main` branch + `/ (root)` pages: enable Pages in GitHub repo settings.
- Alternative: publish from `/docs` or `gh-pages` branch.
